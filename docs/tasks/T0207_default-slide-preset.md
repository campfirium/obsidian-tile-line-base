# T0207 Default Slide Preset

## 2025-11-28
- 已创建分支 `feat/T0207-default-preset` 与工作区 `trees/feat-T0207-default-preset`，工作区 `.git` 指向 `.git/worktrees/feat-T0207-default-preset`，并复制根目录 `.vscode/tasks.json` 以沿用 Ctrl+Shift+B 任务。
- 主题：幻灯片；类型：bug。问题：默认内置 preset 未复用插件的全局默认设置，需要用当前全局默认配置作为初始内置主题。
- 备注：暂不处理语言文件。
