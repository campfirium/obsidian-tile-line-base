# T0219 Tab Reload

## 2025-11-30
- 已创建分支 `feat/T0219-tab-reload` 与工作区 `trees/feat-T0219-tab-reload`，工作区 `.git` 指向 `.git/worktrees/feat-T0219-tab-reload`，并复制根目录 `.vscode/tasks.json` 以沿用 Ctrl+Shift+B 任务。
- 主题：标签页不停重载；类型：bug。场景：其他插件更新后，表格/看板/幻灯片视图所在标签会不断重新加载。
- 备注：暂不处理语言文件。
